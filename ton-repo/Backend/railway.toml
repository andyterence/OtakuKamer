[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn otaku_backend.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/evenements/"
restartPolicyType = "on_failure"